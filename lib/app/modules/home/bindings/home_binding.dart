import 'package:get/get.dart';

/// Page-level bindings. HomeController and ProjectRepository are deleted.
class HomeBinding extends Bindings {
  @override
  void dependencies() {
    // No-op - dependencies are managed globally or dynamically loaded
  }
}

